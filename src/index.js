import React from 'react';
import ReactDOM from 'react-dom/client';
import PawPathsApp from './PawPaths.jsx'; // Import the new component

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PawPathsApp />
  </React.StrictMode>
);
